#include <stdio.h>
#include <stdlib.h>
    int conversion(float argent)
{
     int euro=0;
    return euro=argent*655;
}
int main()
{
    printf("la conversion donne %d\n",conversion(10));
    printf("la conversion donne %d\n",conversion(100));
    return 0;
}
